- изменить логику создания контейнера: если /var/lib/nsbox/$name пустой, создаем там структуру оверлея, если нет пытаемся запустить контейнер из непустого каталога, а при останове смотрим смонтировано что-то в каталог /var/lib/nsbox/$name, если смонтировано, то размонтируем
- разобраться с ошибкой при скаживании многослойных образов. Внесли изменения, но из-за медленного интернета не смогли проверить!
- сделать аргумент `create` для создания контейнера
- попробовать создавать в /var/lib/machines симлинк на каталог merged, а сами хранилища создавать в /var/lib/nsbox. Будет machinectl работать с ними или нет?
- проверить корректность скачивания многослойных docker-образов (были ошибки, вносились изменения, но финальная версия не проверялась)
